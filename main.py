from gui import AppDemo

def main():
    # Creamos la instancia de la interfaz
    app = AppDemo()
    # Iniciamos el bucle principal (mantiene la ventana abierta)
    app.mainloop()

if __name__ == "__main__":
    main()